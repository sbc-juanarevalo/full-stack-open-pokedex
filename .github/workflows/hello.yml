name: Nombre_Workflow

on:
    push:
        branches:
        - master
jobs:
    Hello_Jobs:
        runs-on: ubuntu-20.04
        steps:
        - name: Mostrar mensaje
          run: echo "hola mundo"
        - name: Dime la fecha
          run: date
        - run: ls -l